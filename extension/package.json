{
  "name": "sendsafe-extension",
  "version": "1.0.0",
  "description": "Chrome extension to detect AI-generated copy-paste artifacts in Gmail",
  "scripts": {
    "build": "tsc",
    "watch": "tsc --watch",
    "clean": "rimraf dist"
  },
  "devDependencies": {
    "@types/chrome": "^0.0.254",
    "typescript": "^5.3.0",
    "rimraf": "^5.0.5"
  }
}

